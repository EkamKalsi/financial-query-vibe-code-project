# HTTP client — used by the scraping agent to hit Reddit's .json endpoint
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# Future agents (stubs – install when you reach each phase)
# ---------------------------------------------------------------------------

# Indexing Agent
# sentence-transformers>=2.7.0
# faiss-cpu>=1.8.0          # or faiss-gpu

# Retrieval Agent
# (uses sentence-transformers + faiss from above)

# Reasoning Agent
# openai>=1.30.0            # or anthropic>=0.25.0

# Answering Agent
# (uses openai/anthropic from above)
