# HTTP client — scraping agent
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0

# Embedding agent
sentence-transformers>=3.0.0
numpy>=1.26.0

# ---------------------------------------------------------------------------
# Future agents (stubs – install when you reach each phase)
# ---------------------------------------------------------------------------

# Query agent (LLM)
# anthropic>=0.25.0         # or openai>=1.30.0
